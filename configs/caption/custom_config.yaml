# default_config.yaml —  entrypoint config
# Resolves train_default.py:248 @hydra.main(config_name="custom_config")
# Thin extension of default_config.yaml; add per-experiment overrides below.
#
# Usage:
#   python train_default.py
#   python train_default.py exp.name=s0_baseline
#
# Staged rollout: enable  modules via model_ext.rrm.enabled / model_ext.lcqm.enabled
# See: core/plans/_03_implementation_rollout.md

defaults:
  - default_config
  - _self_

exp:
  name: s0_baseline
